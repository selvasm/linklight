---
- hosts: localhost
  become: false
  tasks:

    - name: Create new  Routers
      azure_rm_virtualmachine_info:
        resource_group: AnsibleTower-Killian-Zoubayer
        name: Router-2
      register: status_result
      until: status_result| failed
      delay: 10
